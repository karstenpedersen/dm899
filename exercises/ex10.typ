= DM899 ex09

== Context

```agda
Context = Nat
```

Context n specifices that the allowed free variables are $0, ..., n - 1$.

Context where $n = 4$.

$
  [*, *, *, *] \
  arrow.b
  [ "Nat", "Bool", "Nat" arrow "Bool", "Bool" ]
$
